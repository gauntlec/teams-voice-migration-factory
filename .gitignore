# Dependencies
node_modules/

# Build output (anchored — do NOT use bare `build/`, it would swallow
# apps/api/src/modules/build/ and similar source folders)
/dist/
/build/
apps/*/dist/
apps/*/build/
packages/*/dist/
coverage/

# Logs & caches
*.log
.turbo/
.vite/
*.tsbuildinfo

# Local env / secrets
.env
.env.local
.env.*.local

# OS
.DS_Store
